---
title: Paginator
description: API reference for the Paginator component
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Terminal } from '@/components/terminal';
import PaginatorDotsExample from '@/components/examples/ui/paginator-dots';
import PaginatorArrowsExample from '@/components/examples/ui/paginator-arrows';
import PaginatorScrollbarExample from '@/components/examples/ui/paginator-scrollbar';
import PaginatorCounterExample from '@/components/examples/ui/paginator-counter';

A scroll position indicator for windowed lists. Supports four display styles: dots, arrows, scrollbar, and counter.

Paginator is a pure display component — it has no focus or interactivity. Pass it the total item count, current scroll offset, and number of visible items. It computes and renders the appropriate indicator. When the total number of items fits within the visible window, Paginator renders nothing.

```tsx title="Standalone Usage"
import { Paginator } from 'giggles/ui';

<Paginator total={100} offset={20} visible={10} style="scrollbar" />
```

### Dots

Shows a row of `○` dots below the list. The current page is shown as `●`.

<Terminal>
  <PaginatorDotsExample />
</Terminal>

### Arrows

Shows `↑` and `↓` indicators above and below the list when there are items outside the visible window.

<Terminal>
  <PaginatorArrowsExample />
</Terminal>

### Scrollbar

Shows a vertical bar beside the list with a thumb that tracks scroll position.

<Terminal>
  <PaginatorScrollbarExample />
</Terminal>

### Counter

Shows a text range like `1–10 of 50` below the list.

<Terminal>
  <PaginatorCounterExample />
</Terminal>

## API Reference

### Paginator

<TypeTable
  type={{
    total: {
      description: 'Total number of items in the list',
      type: 'number',
      required: true,
    },
    offset: {
      description: 'Index of the first visible item (scroll position)',
      type: 'number',
      required: true,
    },
    visible: {
      description: 'Number of items currently visible',
      type: 'number',
      required: true,
    },
    style: {
      description: "Display style. Dots show a row of page indicators, arrows show ↑/↓ indicators, scrollbar shows a vertical thumb, counter shows a text range, none hides the indicator.",
      type: "'dots' | 'arrows' | 'scrollbar' | 'counter' | 'none'",
      default: "'arrows'",
    },
    gap: {
      description: 'Vertical spacing to match the item list gap. Used by the scrollbar style to match the total height of the items.',
      type: 'number',
      default: '0',
    },
    position: {
      description: 'Used by the arrows style to render above or below the list. Ignored by scrollbar and counter styles.',
      type: "'above' | 'below'",
    },
  }}
/>

### PaginatorStyle

```tsx title="Type Definition"
type PaginatorStyle = 'dots' | 'arrows' | 'scrollbar' | 'counter' | 'none';
```
