diff --git a/dist/core.js b/dist/core.js
index 6bb5d974aae6f726a749513c734a01f23e3b83a2..b88352979fafb6d8b4174220aa8ed30c675adf7d 100644
--- a/dist/core.js
+++ b/dist/core.js
@@ -126,8 +126,9 @@ function mountInkInXterm(element, opts) {
   const fitAddon = new FitAddon();
   term.open(opts.container);
   term.loadAddon(fitAddon);
+  let focusTimeoutId;
   if (opts.focus !== false) {
-    setTimeout(() => {
+    focusTimeoutId = setTimeout(() => {
       try {
         term.focus();
       } catch (e) {
@@ -220,13 +221,15 @@ function mountInkInXterm(element, opts) {
   ro.observe(opts.container);
   const onWindowResize = () => resize();
   window.addEventListener("resize", onWindowResize);
-  setTimeout(() => {
+  const readyTimeoutId = setTimeout(() => {
     resize();
     opts.onReady?.();
   }, 200);
   return {
     term,
     unmount: async () => {
+      clearTimeout(focusTimeoutId);
+      clearTimeout(readyTimeoutId);
       try {
         if (instance) {
           instance.unmount();
